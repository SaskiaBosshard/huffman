// TreeNode class to represent a node in the tree
public class TreeNode {
    char ascii;
    int frequency;
    TreeNode left;
    TreeNode right;

    public TreeNode(char ascii, int frequency) {
        this.ascii = ascii;
        this.frequency = frequency;
    }
}